{
  "name": "@simply/extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc -p tsconfig.json && vite build"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.35",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
