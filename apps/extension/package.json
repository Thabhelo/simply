{
  "name": "@simply/extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build && vite build --config vite.content.config.ts && concurrently \"vite build --watch\" \"vite build --config vite.content.config.ts --watch\"",
    "build": "tsc -p tsconfig.json && rm -rf dist && vite build && vite build --config vite.content.config.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.35",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
